# CULTIVATE Mapping Pipeline - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git (already in .gitignore)

# =============================================================================
# Azure Blob Storage Configuration
# =============================================================================
# Your Azure Storage Account name
AZURE_STORAGE_ACCOUNT_NAME=your_storage_account_name

# Azure Storage Account access key (get from Azure Portal)
AZURE_STORAGE_KEY=your_storage_account_key

# Azure Blob Storage container name
AZURE_CONTAINER_NAME=data

# Local directory for Azure sync operations
AZURE_LOCAL_SYNC_DIR=./data_azure_sync

# =============================================================================
# Snowflake Configuration
# =============================================================================
# Snowflake account identifier (e.g., xy12345.us-east-1)
SNOWFLAKE_ACCOUNT=your_account.region

# Snowflake credentials
SNOWFLAKE_USER=your_username
SNOWFLAKE_PASSWORD=your_password

# Snowflake database and schema
SNOWFLAKE_DATABASE=cultivate_db
SNOWFLAKE_SCHEMA=public
SNOWFLAKE_WAREHOUSE=compute_wh

# Snowflake role
SNOWFLAKE_ROLE=ACCOUNTADMIN

# =============================================================================
# OpenAI API Configuration (for FSI classification)
# =============================================================================
# OpenAI API key for GPT-4 classification
OPENAI_API_KEY=sk-your-openai-api-key-here

# Model to use for FSI classification
OPENAI_MODEL=gpt-4o-mini

# =============================================================================
# dbt Configuration (if using dbt Cloud or custom profiles)
# =============================================================================
# dbt profiles directory (optional, defaults to ~/.dbt)
# DBT_PROFILES_DIR=~/.dbt

# =============================================================================
# Project Paths (optional overrides)
# =============================================================================
# Base data directory
# DATA_DIR=./data

# Reports output directory
# REPORTS_DIR=./reports
