[project]
name = "cultivate-mapping-pipeline"
version = "1.0.0"
description = "Data pipeline for mapping and validating food-sharing initiatives across European cities"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2",
    "numpy>=1.26",
    "python-dotenv>=1.0",
    "openpyxl>=3.1",
    "boto3>=1.34",
    "duckdb>=1.0",
    "dbt-duckdb>=1.9",
    "requests>=2.31",
    "beautifulsoup4>=4.12",
]

[project.optional-dependencies]
exploration = [
    "matplotlib>=3.9",
    "seaborn>=0.13",
]
ingestion = [
    "openai-agents>=0.1",
]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
    "pre-commit>=4.0",
]

[tool.pytest.ini_options]
testpaths = ["scripts"]
pythonpath = ["scripts"]

[tool.ruff]
target-version = "py312"
line-length = 120
src = ["scripts", "evaluation", "exploration"]

[tool.ruff.lint]
select = [
    "E",    # pycodestyle errors
    "W",    # pycodestyle warnings
    "F",    # pyflakes
    "I",    # isort
    "UP",   # pyupgrade
    "B",    # flake8-bugbear
    "SIM",  # flake8-simplify
    "RUF",  # ruff-specific rules
]
ignore = [
    "E501",   # line too long (handled by formatter)
    "SIM108", # ternary operator suggestion
]

[tool.ruff.lint.isort]
known-first-party = ["scripts", "evaluation"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
